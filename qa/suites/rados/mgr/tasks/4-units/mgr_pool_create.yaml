overrides:
  ceph:
    log-ignorelist:
      - \(MDS_ALL_DOWN\)
      - \(MDS_UP_LESS_THAN_MAX\)
      - \(FS_WITH_FAILED_MDS\)
      - \(FS_DEGRADED\)

tasks:
  - cephfs_test_runner:
      modules:
        - tasks.mgr.test_mgr_pool_create
